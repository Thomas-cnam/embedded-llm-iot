# Gateway

This folder will later contain the Python 3.x gateway code.

The gateway is planned to:

- Read alerts from the ESP32-C6
- Call a local LLM runtime
- Log responses
- Apply a whitelist safety parser before any command is accepted

For Week 1, no gateway implementation is required yet.


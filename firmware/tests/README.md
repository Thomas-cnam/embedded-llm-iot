# Firmware Test Scripts

This folder contains basic MicroPython peripheral test templates for Week 1 hardware bring-up.

These scripts are placeholders and have not been validated on real hardware yet. GPIO pins must be filled in only after checking the official board documentation. Do not guess pin numbers.

Each script should be tested manually on the ESP32-C6 board, and the result should be recorded in `LOG.md`.

